"use strict"

let myLibrary = [];

function Book(author, title, pageNum, isRead) {
  this.author = author;
  this.title = title;
  this.pageNum = pageNum;
  this.isRead = isRead;
}

function listBooks() {
  for (let book in myLibrary) {

  }
}